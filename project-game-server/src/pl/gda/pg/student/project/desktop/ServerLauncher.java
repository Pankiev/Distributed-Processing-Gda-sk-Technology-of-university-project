package pl.gda.pg.student.project.desktop;

public class ServerLauncher
{
	public static void main(String[] arg)
	{
		// Here is where we start server
		// If start it as libgdx app we can render server-side states
		// So we can debug it easier

		// LwjglApplicationConfiguration config = new
		// LwjglApplicationConfiguration();
		// new LwjglApplication(new GameServer(), config);
	}
}
